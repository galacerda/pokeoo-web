import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  justify-content: center;
  overflow: hidden;
  > div {
    position: fixed;
    top: 0;
    padding: 2rem;
    z-index: 1310;
  }
  z-index: 1310;
`;
